{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "vendor/bin/laravel-octane start --server=swoole --host=0.0.0.0 --port=$PORT"
  }
}
